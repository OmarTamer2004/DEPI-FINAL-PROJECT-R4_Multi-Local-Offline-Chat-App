langchain==0.2.16
langchain-core==0.2.38
langchain-community==0.2.16
langchain-text-splitters==0.2.4

# Groq API backend (replaces ctransformers / local GGUF models)
# langchain-groq 0.1.9 requires langchain-core>=0.2.26,<0.3.0, which is
# why the whole langchain family above was bumped to the 0.2.x line.
langchain-groq==0.1.9
groq==0.9.0
# Pinned: groq==0.9.0 still passes a 'proxies' kwarg to httpx.Client,
# which httpx>=0.28 removed. Without this pin, a fresh install fails at
# ChatGroq() construction with:
#   "Client.__init__() got an unexpected keyword argument 'proxies'"
httpx==0.27.2

chromadb==0.4.22

InstructorEmbedding==1.0.1
sentence-transformers==2.2.2

torch==2.1.2

transformers==4.35.2
huggingface-hub==0.20.3

pypdfium2==4.24.0
librosa==0.10.1
PyYAML==6.0.1

streamlit==1.29.0
streamlit-mic-recorder==0.0.4

# Was already imported unconditionally by app.py (system monitor) but
# missing from requirements.txt, which would break a fresh install.
psutil==5.9.8
